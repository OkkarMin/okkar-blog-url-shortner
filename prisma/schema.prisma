generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Alias {
  alias     String   @id
  url       String
  createdAt DateTime @default(now())
}
